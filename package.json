{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio + writing site (static, GitHub Pages friendly)",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "node build.js && node serve.js"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
